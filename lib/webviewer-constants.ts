/**
 * Default path to WebViewer core files.
 * This path is used when NEXT_PUBLIC_PDFTRON_PATH is not set.
 */
export const DEFAULT_WEBVIEWER_PATH = "/webviewer";

/**
 * Default path to WebViewer core worker files.
 */
export const DEFAULT_WEBVIEWER_CORE_PATH = `${DEFAULT_WEBVIEWER_PATH}/core`;
